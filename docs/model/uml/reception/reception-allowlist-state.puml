@startuml reception-allowlist-state
' Context: reception
' SDD: PROM-RECEPTION-FILTER, SYS-RECEPTION
' Revision: 2026-07-19

title Reception — ReceptionMachine

state Loading
state Ready

[*] --> Loading : LoadAllowlist

Loading --> Ready : AllowlistLoaded
Loading --> Ready : AllowlistLoadFailed

Ready --> Loading : LoadAllowlist\n[after CRUD]
Ready --> Ready : SetSearchQuery
Ready --> Ready : AddAllowedProfile
Ready --> Ready : RemoveAllowedPerson

note right of Ready
  Empty list is still Ready —
  UI shows informational copy.
  allowedProfileIds is a derived view.
end note

@enduml
